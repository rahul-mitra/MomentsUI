import { CapatalizePipe } from './capatalize.pipe';

describe('CapatalizePipe', () => {
  it('create an instance', () => {
    const pipe = new CapatalizePipe();
    expect(pipe).toBeTruthy();
  });
});
